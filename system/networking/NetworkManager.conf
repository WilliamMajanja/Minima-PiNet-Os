# PiNetOS Network Configuration
# /etc/NetworkManager/NetworkManager.conf
# =========================================

[main]
plugins=ifupdown,keyfile
dns=systemd-resolved
rc-manager=symlink

[ifupdown]
managed=true

[device]
wifi.backend=wpa_supplicant

[connection]
ipv6.ip6-privacy=2
connection.stable-id=${CONNECTION}/${BOOT}
